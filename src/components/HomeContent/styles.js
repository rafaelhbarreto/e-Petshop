import styled from "styled-components";

export const ContainerHomeContent = styled.div`
  display: grid; 
  grid-template-columns: 2fr 10fr;
  grid-gap: 1rem; 
  width: 100%;
  max-width: var(--container-width);
  margin: 0 auto;
  padding: 0 15px;
  margin-bottom: 50px;
`;